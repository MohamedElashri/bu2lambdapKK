# Selection Systematic

This study is active and remains part of the top-level analysis workflow.

The authoritative entrypoint is the top-level `analysis/Snakefile` rule
`selection_systematic`, which runs `run_selection_systematic.py`.

The old standalone study-local Snakefile was archived to:

- `analysis/archive/reference_workflows/selection_systematic/Snakefile`
