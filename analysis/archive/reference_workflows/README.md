# Archived Reference Workflows

These files were formerly standalone study-local workflow entrypoints.

They were archived when the top-level workflow was unified because the authoritative DAG now lives in the
top-level `analysis/Snakefile`. They are preserved here only as historical
reference for how those studies used to be launched in isolation.

Archived here:

- `fit_systematics/Snakefile`
- `selection_systematic/Snakefile`

Active code for those studies still lives under:

- `analysis/studies/fit_systematics/run_fit_systematics.py`
- `analysis/studies/selection_systematic/run_selection_systematic.py`
